<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
    <!-- See https://aka.ms/maui-publish-app-store#add-entitlements for more information about adding entitlements.-->
    <dict>
        <!-- App Sandbox must be enabled to distribute a MacCatalyst app through the Mac App Store. -->
        <key>com.apple.security.app-sandbox</key>
        <true/>
        <!-- When App Sandbox is enabled, this value is required to open outgoing network connections. -->
        <key>com.apple.security.network.client</key>
        <true/>
        <!--
            APNs on Mac Catalyst, which spells the key differently from iOS:
            com.apple.developer.aps-environment rather than aps-environment.
            The iOS spelling is silently ignored here, which is a good way to
            ship a head that never registers.

            See the iOS entitlements for why "development" and what a
            distribution build needs instead.
        -->
        <key>com.apple.developer.aps-environment</key>
        <string>development</string>
    </dict>
</plist>

